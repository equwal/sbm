- [X] Don't copy comments, just the bookmark.
- [X] Be able to add bookmarks with a prompter that shows the other bookmarks.

PREVENT DUPLICATES
- [X] prevent duplicates via http(s) difference
- [X] prevent duplicates via www subdomain
- [X] Allow any whitespace as a separator (except newline)
      (the file is now tab separated; bm-migrate converts old files)

- [X] User-defined tags, and new ones typed into the tag menu
- [X] make the use of xclip configurable (other programs exist)
- [X] a dmenu websearch that utilizes bookmarks too
      (text that is no bookmark is an address or a web search; engines file)
- [X] one-screen browsing for dmenu (a second menu: details and actions)
- [X] bookmark preview (fzf: live; dmenu: the second menu)
- [X] a personal web page of the bookmarks, with search (bm-html)
- [X] split into small tools; POSIX sh and POSIX utilities only
- tag filter and sort order from inside the dmenu list
- man pages
